{% set acessoLogin = acesso('login') %}
{% set acessoRegistrar = acesso('registrar') %}
{% if acessoLogin or acessoLogin %}
<div class="row">
	<div class="col-md-12">
	{% if acessoLogin %}
		<div class="form-group">
			<a href="/login" id="buttonGoLogin" class="form-control btn btn-default btn-lg btn-block">Login</a>
		</div>
	{% endif %}
	{% if acessoLogin %}
		<div class="form-group">
			<a href="/registrar" id="buttonGoRegistrar" class="form-control btn btn-primary btn-lg btn-block">Registrar</a>
		</div>
	{% endif %}
	</div>
</div>
{% endif %}