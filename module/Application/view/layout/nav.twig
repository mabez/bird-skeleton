<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><h3><img src="/img/{{logoSite}}" alt="{{nomeSite}}" height="25"/>&nbsp;{{nomeSite}}</h3></a>
        </div>
        <div class="collapse navbar-collapse">
        {% if identificado() is not empty %}
        	<div class="nav navbar-nav navbar-right nav-pills">
        	<li role="presentation" class="active"><a href="#" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{identificacaoUsuario()}}<span class="caret"></span></a>
        	{% if  acesso('conta-registro') or acesso('conta-compras') %}
        	<ul class="dropdown-menu">
        	 {% if  acesso('conta-registro') is not empty %}
			  <li><a href="/conta/registro/">Registro</a></li>
             {% endif %}
        	 {% if  acesso('conta-compras') is not empty %}
			  <li><a href="/conta/compras/">Compras</a></li>
    	     {% endif %}
			</ul>{% endif %}</li>
	        {% if  acesso('admin') is not empty %}
        		<li role="presentation"><a class="btn btn-default" href="/admin/" role="button">
        			<span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
    			</a></li>
    	    {% endif %}
        		<li role="presentation"><a class="btn btn-default" href="/logout?routeRedirect=site" role="button">
        			<span class="glyphicon glyphicon-off" aria-hidden="true"></span>
    			</a></li>
    		</div>
        {% endif %}
        </div><!--/.nav-collapse -->
    </div>
</nav>